[package]
name = "jterm-pty"
version = "0.1.0"
edition.workspace = true
description = "PTY fork/exec management for jterm"

[dependencies]
nix = { version = "0.29", features = ["fs", "process", "signal", "term"] }
libc = "0.2"
log = "0.4"
thiserror = "2"
